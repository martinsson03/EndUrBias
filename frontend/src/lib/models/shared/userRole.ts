// Roles the user can have.
export enum UserRole {
    User = "User",          // Searches for jobs.
    Recruiter = "Recruiter" // Recruits people, also has a recruiter object associated with this user.
};