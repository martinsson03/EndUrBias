import JobViewModel from "@/lib/models/view/jobViewModel";

import { Card, CardAction, CardContent, CardDescription, CardFooter, CardHeader, CardTitle } from "../shadcn/ui/card";

// A visual element on the page containing the job details.
export default function JobDetails(details: JobViewModel) {
    throw new Error("Not implemented!");

    return (
        <div>
            <Card>
                <CardHeader>
                    <CardTitle></CardTitle>
                </CardHeader>
            </Card>
        </div>
    );
}